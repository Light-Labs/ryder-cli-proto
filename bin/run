#!/usr/bin/env node

const oclif_command = require("@oclif/command");
const oclif_cmd_flush = require("@oclif/command/flush");
const oclif_err_handle = require("@oclif/errors/handle");

oclif_command.run().then(oclif_cmd_flush).catch(oclif_err_handle);
